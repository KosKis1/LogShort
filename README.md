# ============================================================
# ШАГ 2: ИНТЕГРАЦИЯ НОВЫХ ТАБЛИЦ И UI
# ============================================================
# Версия: v2.0
# Дата: 02.01.2025
# ============================================================

## 📋 СОДЕРЖИМОЕ АРХИВА

```
step2_files/
├── _patch_step2.py   # Скрипт для модификации главного файла
└── README.md         # Эта инструкция
```

## 🔧 ЧТО ДЕЛАЕТ ПАТЧ

1. **Добавляет импорты V2 модулей** в начало файла
2. **Обновляет заголовки таблиц** (новые колонки)
3. **Добавляет прогресс-бары** в заголовки
4. **Создаёт метод обновления UI** для новой системы

## 📝 ИНСТРУКЦИЯ ПО УСТАНОВКЕ

### 1. Скопируйте файл патча

```
step2_files/_patch_step2.py → C:\Pythone\Log_Short\_patch_step2.py
```

### 2. Запустите патч

```batch
cd C:\Pythone\Log_Short
python _patch_step2.py
```

### 3. Ожидаемый вывод

```
============================================================
🔧 ШАГ 2: ИНТЕГРАЦИЯ НОВЫХ ТАБЛИЦ И UI
============================================================

✅ Все V2 модули найдены

✅ Бэкап создан: auto-short_v095_with_trainer_bridge.py.pre_step2

🔄 Применяем патчи...

✅ Импорты V2 добавлены
✅ Заголовки таблицы 1 обновлены
✅ Заголовки таблицы 2 обновлены
✅ Метка TOP200 обновлена
✅ Метка кандидатов обновлена
✅ _tick_status_lines обновлён
✅ Метод _update_v2_progress_bars добавлен

✅ Файл обновлён: auto-short_v095_with_trainer_bridge.py

============================================================
✅ ШАГ 2 ЗАВЕРШЁН!
============================================================
```

### 4. Запустите сканер для проверки

```batch
python auto-short_v095_with_trainer_bridge.py
```

## ✅ ЧТО ДОЛЖНО ИЗМЕНИТЬСЯ

После применения патча:

1. **Заголовок первой таблицы (TOP200):**
   - Появятся прогресс-бары для трёх уровней сканирования
   - Счётчики: Отобрано X из 200 | 📊 Класс: N | ⚡ Быстрые: N | 🚀 Экстрем: N

2. **Заголовок второй таблицы (Кандидаты):**
   - Прогресс-бар анализа
   - Счётчики статусов: Наблюд | Интерес | Готовн | ВХОД
   - Индикатор триггеров

3. **Колонки таблицы 1 (если USE_NEW_UI=True):**
   - Ранг, Монета, Тип, Позиция%, До хая%, 24ч%, Импульс, V-Спайк, Слабость, Тренд, Зрелость, Критерий

4. **Колонки таблицы 2 (если USE_NEW_UI=True):**
   - Монета, Тип, Критерий, Статус, Скор, До хая%, Exhaustion, Слабость, V-Дин, Тренд, Z-Score, Качество, Вход, SL, TP1, R/R, Осталось

## 🔴 ОТКАТ (если что-то пошло не так)

### Восстановление из бэкапа:

```batch
cd C:\Pythone\Log_Short
copy auto-short_v095_with_trainer_bridge.py.pre_step2 auto-short_v095_with_trainer_bridge.py
```

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. "Файл не найден"
Убедитесь что запускаете из правильной папки:
```batch
cd C:\Pythone\Log_Short
```

### 2. "Модули V2 не найдены"
Выполните Шаг 1 (установка файлов V2).

### 3. "Патчи уже применены"
Это нормально - патч не применяется повторно.

### 4. Ошибки при запуске сканера
1. Соберите логи: `python collect_logs_v2.py`
2. Восстановите бэкап
3. Отправьте логи для анализа

## 📊 ПРОВЕРКА ПОСЛЕ УСТАНОВКИ

### 1. Консоль должна показать:
```
[V2] Новые модули v2 загружены успешно
```

### 2. Заголовки таблиц должны обновляться каждую секунду

### 3. Прогресс-бары должны заполняться

## 📝 ЧТО ДАЛЬШЕ?

После успешного Шага 2:
1. Убедитесь что прогресс-бары работают
2. Проверьте что данные отображаются в новых колонках
3. Сообщите о результатах для получения Шага 3 (новые алгоритмы)

## 🔧 УПРАВЛЕНИЕ ВЕРСИЕЙ

Если нужно временно отключить новый UI:

1. Откройте `core/config_v2.py`
2. Измените `USE_NEW_UI = True` на `USE_NEW_UI = False`
3. Перезапустите сканер

Это вернёт старые заголовки и колонки без отката кода.
