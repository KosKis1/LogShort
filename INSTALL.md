# ИНСТРУКЦИЯ ПО УСТАНОВКЕ V2 FIX

## Что исправлено:

1. **_Start.bat** — теперь запускает `main.py` (модульная версия), 
   а не старый `auto-short_v095_with_trainer_bridge.py`

2. **main.py** — исправлен метод `_update_v2_ui()`:
   - Установлены правильные total_sec для прогресс-баров (120/30/10/5 сек)
   - Исправлен подсчёт статусов и типов монет
   - Добавлена подробная трассировка ошибок

3. **Остальные V2 файлы** — без изменений, для полноты комплекта

## Установка:

1. **Сделай бэкап** всей папки Log_Short (на всякий случай)

2. **Распакуй архив** в папку `C:\Pythone\Log_Short\`:
   - `_Start.bat` → в корень
   - `main.py` → в корень
   - `config_v2.py` → в `core\`
   - `engine_v2.py` → в `core\`
   - `table_headers_v2.py` → в `ui\`

3. **Удали кэш Python** (опционально, но рекомендуется):
   ```
   del /s /q __pycache__
   del /s /q *.pyc
   ```

4. **Запусти** через `_Start.bat`

## Ожидаемый результат:

В консоли должно появиться:
```
Bybit SHORT Scanner v3.0
Mode: MODULAR (main.py)

[TRACE] chart_window imported successfully
[TRACE-V2] V2 модули загружены: USE_NEW_UI = True
[TRACE] MainWindow инициализирован, V2_AVAILABLE = True
```

Интерфейс:
- Заголовок окна: "Bybit SHORT Scanner — Modular v3.0"
- Над первой таблицей: прогресс-бары с эмодзи 🔵 🔄 💰 📊
- Над второй таблицей: статусы кандидатов 🎯

## Если что-то не работает:

1. Проверь консоль на сообщения `[TRACE-V2] Ошибка...`
2. Пришли скриншот консоли
